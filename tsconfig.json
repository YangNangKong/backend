{
    "compilerOptions": {
        "target": "ES6", // "ES6": 컴파일된 JavaScript 코드가 ES6(ECMAScript 2015) 목표 버전으로 생성되도록 지정합니다. 이 옵션을 사용하면 브라우저나 Node.js에서 ES6 문법과 기능을 활용할 수 있습니다.
        "module": "CommonJS", // "CommonJS": 컴파일된 JavaScript 코드가 CommonJS 모듈 시스템을 사용하여 생성되도록 지정합니다. Node.js 환경에서 일반적으로 사용되는 모듈 시스템입니다.
        "outDir": "./dist", // "./dist": 컴파일된 JavaScript 파일들이 저장될 디렉토리를 지정합니다. 일반적으로 컴파일된 파일들은 프로젝트의 루트 디렉토리 아래의 dist 디렉토리에 저장됩니다.
        "rootDir": "./src", // "rootDir": "./src": 소스 TypeScript 파일들의 루트 디렉토리를 지정합니다. TypeScript 컴파일러는 이 디렉토리 내의 파일들을 컴파일하여 outDir에 저장합니다.
        "strict": true, // "strict": true: 엄격한 타입 체크를 활성화합니다. TypeScript의 강력한 타입 시스템을 최대한 활용할 수 있도록 도와줍니다.
        "esModuleInterop": true, // "esModuleInterop": true: ES 모듈과 CommonJS 모듈 간의 상호 운용성을 향상시키기 위해 사용됩니다. 일부 패키지가 ES 모듈 형식으로 익스포트되고 CommonJS 모듈 형식으로 가져오는 경우에 도움이 됩니다.
        "types": ["sequelize"]
    }
}